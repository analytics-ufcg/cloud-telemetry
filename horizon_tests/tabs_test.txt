Seguindo inicialmente os passos do QuickStart do horizon no site:

https://github.com/openstack/horizon/blob/master/doc/source/quickstart.rst

para conexão do horizon. Foi criado uma aba de teste em Admin - Resource Usage (Utilizacao de Recursos) com o nome

de Teste Tab.


Os passos para a criação foram:

1.Baixar o git do horizon: https://github.com/openstack/horizon.git

2.Modificacao do arquivo horizon/openstack_dashboard/dashboards/admin/metering/tabs.py

2.1.Criacao da Class TesteTab


class TesteTab(tabs.Tab):
    name = _("Teste Tab")
    slug = "teste_tab"
    template_name = ("admin/metering/stats.html")
    preload = False

    def _get_flavor_names(request):
        try:
            flavors = api.nova.flavor_list(request, None)
            return [f.name for f in flavors]
        except Exception:
            return ['m1.tiny', 'm1.small', 'm1.medium',
                    'm1.large', 'm1.xlarge']


    def get_context_data(self, request):
        meters = ceilometer.Meters(request)
        if not meters._ceilometer_meter_list:
            msg = _("There are no meters defined yet.")
            messages.warning(request, msg)

        context = {'nova_meters': meters.list_nova(), }
        return context


2.2. Inicializacao da Nova tab na Class CeilometerOverviewTabs

tabs = (DailyReportTab, GlobalStatsTab, TesteTab, )


Obs.: Os arquivos dos templates esta em horizon/openstack_dashboard/dashboards/admin/metering/templates/metering

Modificacoes baseadas na classes ja criadas e pela documentacao do django.
